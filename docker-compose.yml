services:
  python-manager:
    build:
      context: .
      dockerfile: Dockerfile
    # dns:
    #   - 8.8.8.8
    #   - 1.1.1.1
    container_name: python_3_12_trixie
    volumes:
      - ./private:/private
    working_dir: /private
    tty: true
    user: appuser
